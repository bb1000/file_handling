invoer=input('Geef je zin: ')
woorden= invoer.split()
lijst=[]
for woord in woorden:
    if len(woord) >= 3:
        if woord[3].isupper() or woord[-1].isupper():
            lijst.append(woord.upper())
        else:
            print(" ".join(woord))
    else:
        print(" ".join(woord))
